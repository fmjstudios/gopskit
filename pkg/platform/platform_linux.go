//go:build linux

package platform

var (
// InstallDir = filepath.Join("usr", "local", "bin")
// ConfigDir  = filepath.Join(Home(), ".config", "gopskit")
// CacheDir   = filepath.Join(Home(), ".cache", "gopskit")
// LogDir     = filepath.Join(Home(), ".local", "share", "gopskit", "logs")
)
