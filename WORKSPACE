# This WORKSPACE file is merely kept to maintain legacy compatibility by marking this as the root of a Bazel workspace
# The repositories which were previously loaded have been moved to the 'Bzlmod' depenedency management system
# ref: https://bazel.build/external/module

workspace(name = "com_github_fmjstudios_gopskit")
